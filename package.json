{
  "name": "taskyard",
  "version": "0.1.0",
  "type": "module",
  "description": "Agent-first todo. Markdown files and an MCP server.",
  "private": true,
  "workspaces": [
    "packages/cli"
  ],
  "scripts": {
    "build": "npm run build -w taskyard",
    "test": "vitest run",
    "lint": "eslint packages/*/src",
    "typecheck": "tsc --build"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^8.57.1",
    "@typescript-eslint/parser": "^8.57.1",
    "eslint": "^9.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
