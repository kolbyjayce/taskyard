import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { FileStore } from "../store.js";

export function registerStatusTools(server: McpServer, store: FileStore) {
  // TODO: Implement status tools
}